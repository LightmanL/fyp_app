<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Fill in the blanks w/ automated grading", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        
        <% if logged_in? %>
          <!-- Not finish yet. The link below is missing. -->
          <li><%= link_to "", "#", class: "glyphicon glyphicon-home" %></li>
        <% else %>
          <li><%= link_to "", root_path, class: "glyphicon glyphicon-home" %></li>
        <% end %>
        
        <li><%= link_to "", help_path, class: "glyphicon glyphicon-question-sign" %></li>
        
        <% if logged_in? %>
          <li><%= link_to "Users", "#" %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Setting", "#" %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path, method: "delete" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
          <li><%= link_to "Sign up", signup_path %></li>
        <% end %>
        
      </ul>
    </nav>
  </div>
</header>